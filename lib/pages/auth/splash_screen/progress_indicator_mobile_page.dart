import 'package:flutter/material.dart';

class ProgressIndicatorMobilePage extends StatelessWidget {
  const ProgressIndicatorMobilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('lib/assets/images/maxim_logo.png'),
      ),
    );
  }
}
