import 'package:flutter/material.dart';

class ProgressIndicatorDesktopPage extends StatelessWidget {
  const ProgressIndicatorDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('lib/assets/images/maxim_logo.png'),
      ),
    );
  }
}
